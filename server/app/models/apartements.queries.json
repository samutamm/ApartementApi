{
  "createTable": [
    "CREATE TABLE IF NOT EXISTS apartements (",
      "id    SERIAL PRIMARY KEY,",
      "name  TEXT,",
      "address TEXT,",
      "city TEXT,",
      "agent TEXT,",
      "price INTEGER",
      " );"
  ],
  "selectByUsername": [
    "SELECT * FROM persons",
    " WHERE username = ($1)"
  ],
  "selectAll": [
    "SELECT * FROM persons;"
  ],
  "addPerson": [
    "INSERT INTO persons VALUES",
    " (DEFAULT, $1, $2, $3, $4, $5, $6);"
  ]
}
